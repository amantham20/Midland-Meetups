/** Shared between the dynamic loading shell (page.tsx) and SquadClient so the
 *  header doesn't shift when the client bundle lands. */
export const SQUAD_HEADER = {
  kicker: "Who's in it",
  title: "The Squad",
  lede: "The people who show up. Tap anyone to read their profile.",
};
